generator client {
  provider = "prisma-client-js"
  output   = "../../generated/mongo-client"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model Product {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  name  String
  price Int
}
